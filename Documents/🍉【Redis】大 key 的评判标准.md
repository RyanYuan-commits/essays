**在一般的业务场景下（并发和容量要求都不大）**
- 单个string的 value 大于 1MB
- 容器元素超过 10000
**在高并发且低延迟的场景**
- 单个 string 的 value 大于 10KB
- 容器元素超过 5000 或整体 value 大于 10MB
**阿里云 Redis**
- string 的 key 为 5MB
- Key 中成员数过多：容器元素超过 10000 个
- Key 中成员的数据量过大：一个 Hash 的 key，成员数量 1000，但是总大小为 100MB