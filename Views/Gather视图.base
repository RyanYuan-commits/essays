views:
  - type: cards
    name: 视图
    filters:
      or:
        - file.basename.contains("【Main】")
        - file.basename.contains("(Main)")
    order:
      - file.basename
    sort:
      - property: file.mtime
        direction: DESC
    cardSize: 260
